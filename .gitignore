.env
.DS_Store

# The directories are now symlinks because the home partition is too small and the work partition is read-only. They point to the container partition now. 
target
logs
threads
python_pickles

# I keep the testing notebook the same between version unless something changes
testing_copy.ipynb
